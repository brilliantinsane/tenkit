# Copy this file to .env.local for local Expo commands when you want a
# specific App Variant without prefixing the command.
#
# The Tenkit build command writes .env.local from EAS environment variables.
# Keep .env.local uncommitted.

APP_VARIANT_SLUG=first-tenant
