import { type AppVariant } from '@/types/app-variant';

export const defaultAppVariantId = 1;

export const appVariants = [
  {
    appVariantId: {{appVariants.[0].appVariantId}},
    // Replace with the actual EAS Project Slug after creating or linking the project.
    slug: '{{appVariants.[0].slug}}',
    name: {{{appVariants.[0].displayNameStringLiteral}}},
    version: '1.0.0',
    scheme: '{{appVariants.[0].scheme}}',
    bundleIdentifier: '{{appVariants.[0].bundleIdentifier}}',
    packageName: '{{appVariants.[0].packageName}}',
    theme: {
      accent: {{{appVariants.[0].accentStringLiteral}}},
    },
    eas: {
      projectId: '',
    },
  },
  {
    appVariantId: {{appVariants.[1].appVariantId}},
    // Replace with the actual EAS Project Slug after creating or linking the project.
    slug: '{{appVariants.[1].slug}}',
    name: {{{appVariants.[1].displayNameStringLiteral}}},
    version: '1.0.0',
    scheme: '{{appVariants.[1].scheme}}',
    bundleIdentifier: '{{appVariants.[1].bundleIdentifier}}',
    packageName: '{{appVariants.[1].packageName}}',
    theme: {
      accent: {{{appVariants.[1].accentStringLiteral}}},
    },
    eas: {
      projectId: '',
    },
  },
] as const satisfies readonly AppVariant[];
