import { Text, View } from 'react-native';

import { useAppVariantConfig } from '@/hooks/use-app-variant-config';

export default function HomeScreen() {
  const { appVariant, theme } = useAppVariantConfig();

  return (
    <View className="flex-1 items-center justify-center gap-4 bg-bg-dark px-6">
      <Text className="text-sm font-semibold uppercase tracking-normal" style=\{{ color: theme.accent }}>
        Tenkit
      </Text>
      <Text className="text-center text-4xl font-bold text-text">
        White Label App
      </Text>
      <View className="min-w-56 gap-1">
        <Text selectable className="font-mono text-sm text-text">
          App Variant: {appVariant.slug}
        </Text>
      </View>
      <Text className="max-w-sm text-center text-base text-text-muted">
        A generated Expo SDK 56 app with White Label app configuration applied.
      </Text>
    </View>
  );
}
