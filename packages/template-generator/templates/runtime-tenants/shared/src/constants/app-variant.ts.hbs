import { type AppVariant } from '@/types/app-variant';

export const appVariant = {
  appVariantId: 1,
  slug: 'acme-app',
  name: 'Acme App',
  version: '1.0.0',
  scheme: 'acmeapp',
  bundleIdentifier: 'com.example.acmeapp',
  packageName: 'com.example.acmeapp',
  theme: {
    {{#if hasAccentOverride}}
    accent: {{{accentOverrideStringLiteral}}},
    {{else}}
    accent: '#eb2556',
    {{/if}}
  },
  eas: {
    projectId: '',
  },
  runtimeTenantAccess: {
    defaultRuntimeTenantId: 100,
    allowedRuntimeTenantIds: [100, 101, 102],
  },
} as const satisfies AppVariant;
